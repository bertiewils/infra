---
- include: users.yml
  become: true
  tags: users

- include: sshd.yml
  become: true
  when: ansible_pub_key.failed is not true
  tags: sshd

- include: programs.yml
  become: true
  tags: programs

- include: motd.yml
  become: true
  tags: motd
